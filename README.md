# The program consists of a chat between the server and the clients. 
There can be many clients.The program uses thread distribution, so sending and receiving packets occurs independently of each other
